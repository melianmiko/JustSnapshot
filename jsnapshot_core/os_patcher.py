def patch_fstab_of_backup(restored_volumes, restore_map, callback):
    """
    Find and patch fstab in restored subvolumes.

    :param restored_volumes: List of restored subvolumes
    :param restore_map: Restore map (recover_paths file)
    :param callback: User callback class
    :return: nothing
    """
    pass
